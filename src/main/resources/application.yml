spring:
  datasource:
    url: jdbc:postgresql://cadastro-db:5432/cadastro
    username: gavioli
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect